# Formulario de inscripción — Cancha Uno

Formulario público para anotarse en la lista de espera de clases de pádel.

Este repo solo contiene el formulario ([index.html](index.html)); el sistema de
administración es privado. Cada push a `main` se publica automáticamente en
GitHub Pages.
